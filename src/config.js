const config = {
    mainWidth: 1100,
    routes: {
        home: '/',
        profile: '/following',
        postDetail: '/:nickname/video/:videoId',
        following: '/',
    }
}

export default config
